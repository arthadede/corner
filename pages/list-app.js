import { AppList } from '../src/views';
import { BlankLayout } from '../src/layouts';

AppList.displayName = 'AppListPage';
AppList.pageTitle = 'AppList';
AppList.Layout = BlankLayout;

export default AppList;
